{
  "extends": [
    "config:base"
  ],
  "packageRules": [
    {
      "matchManagers": ["docker-compose", "npm", "nvm","github-actions","docker-file","regex"],
      "groupName": "{{manager}}"
    }
  ],
  "regexManagers": [
    {
      "description": "Update ARG versions of services in Dockerfiles",
      "fileMatch": [
        "(^|/|\\.)Dockerfile$",
        "(^|/)Dockerfile\\.[^/]*$"
      ],
      "matchStrings": [
        "#\\s*renovate:\\s*datasource=(?<datasource>.*?) depName=(?<depName>.*?)( versioning=(?<versioning>.*?))?\\sARG .*?_VERSION=(?<currentValue>.*)\\s"
      ],
      "versioningTemplate": "docker"
    }
  ]
}
