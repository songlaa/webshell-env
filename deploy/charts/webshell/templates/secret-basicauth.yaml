---
apiVersion: v1
kind: Secret
metadata:
  name: basic-auth
  labels:
    {{- include "webshell.labels" . | nindent 4 }}
type: Opaque
data:
  auth: {{ (htpasswd .Values.student .Values.password) | b64enc | quote }}
